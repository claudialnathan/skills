@AGENTS.md

<!--
`AGENTS.md` is the one source of project instructions, read by every harness. This file imports it so Claude Code reads the same thing.

Add a section below only for something true of Claude Code and nothing else — a hook, a settings path, a plugin-scope collision. Never restate a rule from `AGENTS.md` here; two copies of one rule is one rule that will disagree with itself.
-->
