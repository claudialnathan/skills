# Owner approval template

`checkpoint-3a.example.json` documents the schema and control fields required
before a model-backed evaluation can start. It authorizes no call.

Create real approvals under the ignored
`working/token-efficiency/approvals/` directory. Do not commit owner names,
account details, spend ledgers, consumed budgets, or retry history as reusable
fixtures.
