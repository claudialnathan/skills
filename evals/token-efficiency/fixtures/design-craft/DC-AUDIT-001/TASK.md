# Dashboard audit fixture

Review `index.html` and `styles.css` as source evidence. This is audit-only:
do not edit files.

The dashboard should make the account health summary the first read, keep
secondary metrics comparable, and leave destructive administration visually
subordinate. No browser, screenshot, or contrast tool is available, so source
observations must not be reported as rendered proof.
