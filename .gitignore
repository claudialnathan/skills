.DS_Store
Icon?

# Machine-local session docs — never committed
CLAUDE.local.md
TODO.md
HANDOFF-*.md
working/
scratch/

# Machine-local Claude Code state
.claude/settings.local.json
# Cross-tool symlink dir — regenerated by bin/sync-cross-tool, never committed
.claude/skills/

# Eval / probe RUN OUTPUTS — keep the runner scripts (evals/*.sh, evals/grade-probe.js),
# the manifest (evals/*-batch.txt), and each skill's evals/probes.md spec tracked; do NOT
# commit produced output. (recorded baselines are re-runnable and --safe-mode leaks account
# context — the record of truth is the probes.md verdict table + CHANGELOG.)
.eval-runs/
**/evals/runs/
**/evals/baselines/
