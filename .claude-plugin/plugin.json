{
  "name": "skills",
  "description": "Applied skills: shadcn-tailwind (shadcn 4 + Tailwind v4), the design family (designer, improve-composition, improve-layout, improve-motion), optimistic-ui (write local first, reconcile with the network), quality-audit (stack-aware repository and launch-readiness audit), onboard (install the portable project harness), handover (one live handoff) and wire-checks (get a repository's automated checks running), use-browser and inspect-web (check and read a running page), saltintesta (prose tone), flavored-md (GFM form-matching), zoom-out (strategic project review), ship (commit pipeline whose message is context for the next agent, with a folded decision-log step). Narrow, stack-specific discipline.",
  "author": {
    "name": "Claudia Nathan"
  },
  "license": "MIT",
  "keywords": [
    "claude-code",
    "skills",
    "applied"
  ]
}
